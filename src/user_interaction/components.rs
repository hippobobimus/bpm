/// A marker component that indicates the Entity is associated with the player.
pub struct Player;

/// A marker component that indicates the Entity can be controlled by the keyboard.
pub struct KeyboardControlled;
