/// A marker component for the Entity that holds the FPS debugging text.
pub struct FpsDebugText;

/// A marker component for the Entity that holds the Player debugging text.
pub struct PlayerDebugText;
